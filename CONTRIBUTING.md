# Contributing

The design is frozen — bug fixes and docs are welcome via issues.
Measurement improvements and integration wiring carry the same bar: every
change ships with a gate, `npm run verify` stays exit 0, and the claims
linter stays green.
